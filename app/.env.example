# Copy this file to `.env` and fill in from your Supabase project (Settings -> API).
# Without these, the app runs on the built-in mock data (fine for web/dev/the test).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
